using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AnimPlay : MonoBehaviour
{
    public Animator nimChim;
    // Start is called before the first frame update
    void Start()
    {
        nimChim.Play("shid");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
